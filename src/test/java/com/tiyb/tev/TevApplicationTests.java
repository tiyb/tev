package com.tiyb.tev;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

/**
 * Set of unit tests for overall application. Nothing actually implemented here.
 *
 */
@RunWith(SpringRunner.class)
@SpringBootTest
public class TevApplicationTests {

	/**
	 * Empty test. The only reason it's not being deleted is that it at least
	 * verifies that JUnit is working; there will always be at least one test that
	 * passes.
	 */
	@Test
	public void contextLoads() {
	}

}
